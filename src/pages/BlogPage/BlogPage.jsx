import { Box } from '@mui/material';
import React from 'react';

const BlogPage= () => {
    return (
        <Box className="section">
            blogs
        </Box>
    );
};

export default BlogPage;